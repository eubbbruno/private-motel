import {PageShell,PageIntro,Photo,FinalCTA} from '../../src/components/Editorial';
import {meals} from '../../src/data/content';
export default function CortesiasContent(){return <PageShell><PageIntro eyebrow="Hospitalidade à mesa" title="Saboreie o seu tempo." description="Café da manhã, almoço executivo e chá da tarde. Cortesias para completar o seu momento no Private."/><div className="wrap">{meals.map((m,i)=><section className="editorial-row" key={m.title}><Photo src={m.image} alt={m.title+' do Private Motel'} priority={i===0}/><div><p className="eyebrow">0{i+1} / Cortesia Private</p><h2>{m.title}</h2><p>{m.horario}</p><ul>{m.cardapio.map(item=><li key={item}>{item}</li>)}</ul></div></section>)}<p className="note">Em relação às refeições gratuitas e promoções, somente é permitido uma (1) por período, assim sendo não cumulativas.</p></div><FinalCTA/></PageShell>;}
